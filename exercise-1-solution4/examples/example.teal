fun f(x, y) = {
    return x + 1;
}

fun main() = {
    var a := f(1, 0);
    var b := new array[int](8);
    var x := b[0];
    b[0] := 3;
    var y;
    if (y < 5) {
        return 1;
    }
    return 0;
}
